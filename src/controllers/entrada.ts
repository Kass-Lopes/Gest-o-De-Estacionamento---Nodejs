import { I_EntradaController } from "./protoclos";

export class Entrada implements I_EntradaController {
    handle() {
        throw new Error("Method not implemented.");

        //validar requisição
        //mandar a requição para o repository
    }
}